package Week4;

public abstract class Abstract {
    // any class which at-least has one abstract method
    // the method is called abstract method if it does not have implementation in that class

    public abstract void my_method();

    // if there is abstract method inside your class then class itself has to be an abstract
    // and abstract class can not be instantiated means we cant not "directly" make an object of that class

}
